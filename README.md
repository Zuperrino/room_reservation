# Система бронирования переговорок

API для управления переговорными комнатами и бронированиями на базе FastAPI.

## Возможности

- 🔐 Аутентификация и авторизация пользователей (fastapi-users)
- 🏢 Управление переговорными комнатами
- 📅 Бронирование переговорок с проверкой пересечений
- 👤 Роли пользователей (обычные пользователи и суперпользователи)
- 🛠️ Админ-панель (SQLAdmin) для управления данными

## Технологии

- **FastAPI** — веб-фреймворк
- **SQLAlchemy 2.0** — ORM с async поддержкой
- **SQLite** — база данных
- **Alembic** — миграции БД
- **fastapi-users** — аутентификация
- **SQLAdmin** — админ-панель
- **Pydantic** — валидация данных

## Установка

1. Клонируй репозиторий `https://github.com/Zuperrino/room_reservation`
2. Создай виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate  # Windows
   ```
3. Установи зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Создай файл `.env` в корне проекта:
   ```env
   На примере .env.example
   ```
5. Примени миграции:
   ```bash
   alembic upgrade head
   ```

## Запуск

```bash
uvicorn app.main:app --reload
```

API будет доступен по адресу: `http://127.0.0.1:8000`

Документация: `http://127.0.0.1:8000/docs`

Админ-панель: `http://127.0.0.1:8000/admin`

## API Endpoints

- `/auth/*` — аутентификация (регистрация, вход, выход)
- `/meeting_rooms` — управление переговорными комнатами
- `/reservations` — управление бронированиями
- `/users` — управление пользователями

## Структура проекта

```
app/
├── api/           # API роутеры и эндпоинты
├── core/          # Конфигурация, БД, админка
├── crud/          # CRUD операции
├── models/        # SQLAlchemy модели
└── schemas/       # Pydantic схемы
```

